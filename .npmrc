# @bosco98/* packages come from GitHub Packages. Even public packages there
# need auth to download: put a token with read:packages in your ~/.npmrc as
#   //npm.pkg.github.com/:_authToken=YOUR_TOKEN
# (CI uses the built-in GITHUB_TOKEN.)
@bosco98:registry=https://npm.pkg.github.com
